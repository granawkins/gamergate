[1mdiff --git a/backend/main.py b/backend/main.py[m
[1mindex 5899ed8..e3f5e6f 100644[m
[1m--- a/backend/main.py[m
[1m+++ b/backend/main.py[m
[36m@@ -39,19 +39,9 @@[m [masync def get_games():[m
     return list(_db["games"].values())[m
 [m
 [m
[31m-@app.post("/games/check-name")[m
[31m-async def check_game_name(request: Request):[m
[31m-    """[m
[31m-    Check if a game name is unique in the database.[m
[31m-    Returns true if the name is available, false if it's already taken.[m
[31m-    If current_game_id is provided, it will exclude that game from the check.[m
[31m-[m
[31m-    Request body:[m
[31m-    {[m
[31m-        "name": str,[m
[31m-        "current_game_id": Optional[str][m
[31m-    }[m
[31m-    """[m
[32m+[m[32m@app.post("/games/update-name")[m
[32m+[m[32masync def update_game_name(request: Request):[m
[32m+[m[32m    """Update the name, if it's not a duplicate of another game."""[m
     data = await request.json()[m
     name = data.get("name")[m
     current_game_id = data.get("current_game_id")[m
[36m@@ -64,18 +54,39 @@[m [masync def check_game_name(request: Request):[m
     # Check if the name is already taken by another game[m
     for id, game in _db["games"].items():[m
         if game["name"] == name and id != current_game_id:[m
[31m-            return {"available": False}[m
[32m+[m[32m            return {"successful": False}[m
[32m+[m
[32m+[m[32m    # If current_game_id is provided, update the game's name[m
[32m+[m[32m    if current_game_id and current_game_id in _db["games"]:[m
[32m+[m[32m        _db["games"][current_game_id]["name"] = name[m
[32m+[m[32m        _db["games"][current_game_id]["updated_at"] = datetime.now().isoformat()[m
[32m+[m[32m        await db.set(_db)[m
[32m+[m[32m        return {"successful": True}[m
 [m
[32m+[m[32m    # For backward compatibility[m
     return {"available": True}[m
 [m
 [m
 @app.get("/games/{game_name}/play")[m
 async def serve_game(game_name: str):[m
     """Serve the HTML file for a specific game with added resize handling."""[m
[31m-    game_dir = GAMES_PATH / game_name[m
[32m+[m[32m    # Find the game ID from the name in the database[m
[32m+[m[32m    _db = await db.get()[m
[32m+[m[32m    game_id = None[m
[32m+[m
[32m+[m[32m    for id, game in _db["games"].items():[m
[32m+[m[32m        if game["name"] == game_name:[m
[32m+[m[32m            game_id = id[m
[32m+[m[32m            break[m
[32m+[m
[32m+[m[32m    if game_id is None:[m
[32m+[m[32m        raise HTTPException(status_code=404, detail=f"Game '{game_name}' not found")[m
[32m+[m
[32m+[m[32m    # Use the game ID to locate the game directory[m
[32m+[m[32m    game_dir = GAMES_PATH / game_id[m
 [m
[31m-    # First check if there's a file named after the game[m
[31m-    game_file = game_dir / f"{game_name}.html"[m
[32m+[m[32m    # First check if there's a file named index.html (most common)[m
[32m+[m[32m    game_file = game_dir / "index.html"[m
     if not game_file.exists():[m
         # If not, look for any HTML file in the directory[m
         html_files = list(game_dir.glob("*.html"))[m
